import LoginScreen from './LoginScreen'; // Import the LoginScreen component for re-export

/**
 * Index file that exports the LoginScreen component as the default export,
 * serving as the entry point for importing the LoginScreen component throughout the application.
 */
export default LoginScreen;