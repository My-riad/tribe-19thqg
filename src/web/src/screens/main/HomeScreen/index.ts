// src/web/src/screens/main/HomeScreen/index.ts
// Barrel file that exports the HomeScreen component for use throughout the application.
// This file simplifies imports by providing a single entry point for the HomeScreen component.

import HomeScreen from './HomeScreen'; // Import the HomeScreen component for re-export

// Re-export the HomeScreen component as the default export for use in navigation and other parts of the application
export default HomeScreen;