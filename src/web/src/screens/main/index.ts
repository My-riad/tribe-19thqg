import HomeScreen from './HomeScreen'; // Import the HomeScreen component for re-export
import DiscoverScreen from './DiscoverScreen'; // Import the DiscoverScreen component for re-export
import EventsScreen from './EventsScreen'; // Import the EventsScreen component for re-export
import ChatScreen from './ChatScreen'; // Import the ChatScreen component for re-export
import ProfileScreen from './ProfileScreen'; // Import the ProfileScreen component for re-export

/**
 * Export the HomeScreen component for use in navigation and other parts of the application
 */
export { HomeScreen };

/**
 * Export the DiscoverScreen component for use in navigation and other parts of the application
 */
export { DiscoverScreen };

/**
 * Export the EventsScreen component for use in navigation and other parts of the application
 */
export { EventsScreen };

/**
 * Export the ChatScreen component for use in navigation and other parts of the application
 */
export { ChatScreen };

/**
 * Export the ProfileScreen component for use in navigation and other parts of the application
 */
export { ProfileScreen };