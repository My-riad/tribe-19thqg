import NotificationScreen from './NotificationScreen'; // Import the NotificationScreen component for re-export

/**
 * Export file for the NotificationScreen component, making it available for import throughout the application.
 * This file serves as a centralized entry point for importing notification screens throughout the application,
 * simplifying imports by allowing consumers to import from a single location rather than from individual screen directories.
 */
export default NotificationScreen; // Export the NotificationScreen component as the default export