import PrivacySettingsScreen from './PrivacySettingsScreen'; // react-native v0.70.0

/**
 * Index file that exports the PrivacySettingsScreen component for use throughout the application.
 * This file serves as the entry point for importing the PrivacySettingsScreen component.
 */

// Export the PrivacySettingsScreen component as the default export to make it available to other parts of the application
export default PrivacySettingsScreen;