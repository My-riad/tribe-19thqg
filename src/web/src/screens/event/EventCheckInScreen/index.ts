// Import the EventCheckInScreen component for re-export
import EventCheckInScreen from './EventCheckInScreen'; // EventCheckInScreen component for checking in to events

// Re-export the EventCheckInScreen component as the default export for simplified imports
export default EventCheckInScreen;