import EventSuggestionScreen from './EventSuggestionScreen';

/**
 * Exports the EventSuggestionScreen component as the default export,
 * serving as the entry point for the EventSuggestionScreen module.
 */
export default EventSuggestionScreen;