// Import the AuthForm component
import AuthForm from './AuthForm'; // Import the AuthForm component
import { AuthFormProps, FormType } from './AuthForm'; // Import the AuthFormProps interface and FormType type

// Re-export the AuthForm component as the default export
export default AuthForm;

// Re-export the AuthFormProps interface and FormType type
export type { AuthFormProps, FormType };