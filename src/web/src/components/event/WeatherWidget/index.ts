/**
 * Export the WeatherWidget component for easy importing throughout the application.
 * This barrel file simplifies imports by allowing consumers to import directly from 
 * the component folder rather than specifying the exact file.
 */
export { default, WeatherWidgetProps } from './WeatherWidget';