// Import the EventCard component for re-export
import EventCard from './EventCard';
// Import the props interface for the EventCard component for re-export
import { EventCardProps } from './EventCard';

// Re-export the EventCard component as the default export
export default EventCard;

// Export the props interface for the EventCard component
export type { EventCardProps };