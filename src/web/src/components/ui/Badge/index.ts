import Badge, { 
  BadgeProps, 
  BadgeSize, 
  BadgeVariant, 
  BadgeType 
} from './Badge';

// Re-export types for use throughout the application
export type { 
  BadgeProps, 
  BadgeSize, 
  BadgeVariant, 
  BadgeType 
};

// Re-export the Badge component as the default export
export default Badge;