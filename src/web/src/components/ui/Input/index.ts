/**
 * Input component entry point
 * 
 * This file exports the Input component and associated types for use throughout
 * the application. The Input component is a versatile text input that supports
 * multiple variants, sizes, and states as specified in the design system.
 */

import Input from './Input';
import { InputProps, InputVariant, InputSize } from './Input';

export type { InputProps, InputVariant, InputSize };
export default Input;