# Prevent environment-specific configuration files with sensitive information from being committed
.env
.env.local
.env.development
.env.staging
.env.production
.env.test
*.env

# Dependencies
# Exclude third-party dependencies that should be installed during build rather than committed
node_modules/
node_modules_*/
*/node_modules/

# Build Artifacts
# Exclude compiled code and build outputs that should be generated during build process
dist/
*/dist/
build/
*.tsbuildinfo

# Logs
# Exclude log files that are generated during development and runtime
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Cache
# Exclude cache files generated by various tools
.npm
.eslintcache
.node_repl_history
.yarn-integrity
.cache/
.parcel-cache/
.rts2_cache_*/
.rpt2_cache/

# Test Coverage
# Exclude test coverage reports generated during testing
coverage/
.nyc_output/
jest-coverage/

# Database
# Exclude local database files and development migrations
*.sqlite
*.sqlite3
prisma/migrations/dev/
prisma/dev.db
prisma/dev.db-journal

# Security-sensitive Files
# Prevent accidental commit of SSL/TLS certificates and private keys
*.pem
*.key
*.crt
*.cert
*.keystore
*.jks

# Docker
# Exclude Docker-related data volumes and temporary files
.docker/
docker-volumes/
postgres_data/
redis_data/

# IDE and Editor
# Exclude IDE and editor configuration files and temporary files
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Temporary Files
# Exclude temporary files created during development or build
tmp/
temp/
*.tmp
*.temp

# Miscellaneous
.DS_Store
.AppleDouble
.LSOverride
.directory
Thumbs.db
ehthumbs.db