import challengeController from './challenge.controller';
import engagementController from './engagement.controller';
import promptController from './prompt.controller';

/**
 * Export the challenge controller for handling challenge-related HTTP requests
 */
export { challengeController };

/**
 * Export the engagement controller for handling engagement-related HTTP requests
 */
export { engagementController };

/**
 * Export the prompt controller for handling prompt-related HTTP requests
 */
export { promptController };